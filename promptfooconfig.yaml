# Root promptfoo configuration
# Run: npx promptfoo eval

description: "FTM Skills Eval Suite"

# Include eval configs from individual skills
evaluateOptions:
  maxConcurrency: 2

# Default provider for all evals
providers:
  - id: "exec:claude --print"
    label: "claude-code"
